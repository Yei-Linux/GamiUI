import {
  rounded,
  boxShadow,
  breakspointSizes,
  font,
  opacity,
  sizes,
  spacing,
  zIndex,
} from '.'

export const defaultTokens = {
  rounded,
  boxShadow,
  breakspointSizes,
  font,
  opacity,
  sizes,
  spacing,
  zIndex,
}
