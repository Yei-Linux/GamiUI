import {
  borderRadius,
  boxShadow,
  breakspointSizes,
  font,
  opacity,
  sizes,
  spacing,
  zIndex,
} from '.'

export const tokens = {
  borderRadius,
  boxShadow,
  breakspointSizes,
  font,
  opacity,
  sizes,
  spacing,
  zIndex,
}
