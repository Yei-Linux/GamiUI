import {
  borderRadius,
  boxShadow,
  breakspointSizes,
  font,
  opacity,
  sizes,
  spacing,
  zIndex,
} from '.'

export const defaultTokens = {
  borderRadius,
  boxShadow,
  breakspointSizes,
  font,
  opacity,
  sizes,
  spacing,
  zIndex,
}
